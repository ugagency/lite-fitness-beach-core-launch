services:
  - type: static
    name: lite-fitness
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - source: /assets/*
        destination: /assets/*
      - source: /*
        destination: /index.html